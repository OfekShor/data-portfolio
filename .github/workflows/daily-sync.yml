name: Daily Sync Google Sheets to SQLite

on:
  schedule:
    - cron: '0 3 * * *'  # Every day at 3:00 AM UTC
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r my-portfolio/backend/requirements.txt

      - name: Create credentials.json from secret
        env:
          CREDENTIALS_JSON: ${{ secrets.GSHEET_CREDENTIALS }}
        run: |
          echo "$CREDENTIALS_JSON" > my-portfolio/backend/credentials.json

      - name: Run sync script
        run: python my-portfolio/backend/sync_sheet_to_sql.py

    #  - name: Commit and push updated DB and CSVs
     #   run: |
      #    git config user.name "github-actions"
       #   git config user.email "github-actions@github.com"
        #  git add my-portfolio/stocks.db my-portfolio/csv_exports/*.csv
         # git commit -m "🔄 Auto-sync $(date +'%Y-%m-%d %H:%M')" || echo "No changes to commit"
          #git push
